spring:
  application:
    name: comment-service

  profiles:
    active: dev

  config:
    import: "optional:configserver:"

  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER